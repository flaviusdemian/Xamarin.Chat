<views:MvxWindowsPage
    x:Class="XamChat.UWP.Views.LoginView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:views="using:MvvmCross.WindowsCommon.Views"
    mc:Ignorable="d">

    <Grid Background="{StaticResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel>
            <TextBlock Margin="12" Text="Email" />
            <TextBox Margin="12" 
                     Text="{Binding Email, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <TextBlock Margin="12" Text="Password" />
            <TextBox Margin="12" Text="{Binding Password, Mode=TwoWay}" />
            <Button Margin="12 24" Width="200" Content="Login" x:Uid="btn_login"
                    HorizontalAlignment="Left"/>

            <TextBlock Margin="12" Text="{Binding Email, Mode=TwoWay}" />

        </StackPanel>
    </Grid>
</views:MvxWindowsPage>
